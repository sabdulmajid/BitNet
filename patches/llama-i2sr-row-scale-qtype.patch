diff --git a/src/ggml-bitnet-mad.cpp b/src/ggml-bitnet-mad.cpp
index 4ba9d65..384b4c7 100644
--- a/src/ggml-bitnet-mad.cpp
+++ b/src/ggml-bitnet-mad.cpp
@@ -195,6 +195,140 @@ size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_
 #endif
 }
 
+size_t quantize_i2_sr(const float * src, void * dst, int64_t nrow, int64_t n_per_row, const float * quant_weights) {
+#if defined(__AVX__) || defined(__AVX2__) || defined(__AVX512F__) || defined(__SSSE3__)
+#if defined(ACT_PARALLEL)
+    size_t row_size = ggml_row_size(GGML_TYPE_I2_SR, n_per_row);
+    int64_t n = nrow * n_per_row;
+
+    uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
+        }
+    }
+
+    memset(dst, 0, n * sizeof(uint8_t) / 4);
+
+    uint8_t* i2_weight = (uint8_t*)dst;
+    for (int64_t i = 0; i < n / QK_I2_S; i++) {
+        for (int64_t j = 0; j < QK_I2_S; j++) {
+            int64_t group_idx = j / 32;
+            int64_t group_pos = j % 32;
+            uint8_t temp = (q8[i * QK_I2_S + j] << (6 - 2 * group_idx));
+            i2_weight[i * 32 + group_pos] |= temp;
+        }
+    }
+
+    free(q8);
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
+#else
+    assert((nrow % 4) == 0 && "quantize_i2_sr_1x4 requires nrow % 4 == 0");
+
+    size_t row_size = ggml_row_size(GGML_TYPE_I2_SR, n_per_row);
+    int64_t n = nrow * n_per_row;
+
+    uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
+        }
+    }
+
+    uint8_t* out = (uint8_t*)dst;
+    memset(out, 0, (size_t)(n / 4));
+
+    int64_t nrow4 = nrow / 4;
+    for (int64_t rg = 0; rg < nrow4; rg++) {
+        int64_t r0 = rg * 4 + 0;
+        int64_t r1 = rg * 4 + 1;
+        int64_t r2 = rg * 4 + 2;
+        int64_t r3 = rg * 4 + 3;
+
+        int64_t base = rg * n_per_row;
+
+        for (int64_t col = 0; col < n_per_row; col++) {
+            uint8_t q0 = q8[r0 * n_per_row + col];
+            uint8_t q1 = q8[r1 * n_per_row + col];
+            uint8_t q2 = q8[r2 * n_per_row + col];
+            uint8_t q3 = q8[r3 * n_per_row + col];
+
+            uint8_t packed = (uint8_t)((q0 << 6) | (q1 << 4) | (q2 << 2) | (q3 << 0));
+            out[base + col] = packed;
+        }
+    }
+
+    free(q8);
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
+#endif
+#elif defined(__ARM_NEON)
+    size_t row_size = ggml_row_size(GGML_TYPE_I2_SR, n_per_row);
+    int64_t n = nrow * n_per_row;
+
+    uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
+        }
+    }
+
+    memset(dst, 0, n * sizeof(uint8_t) / 4);
+
+    uint8_t* i2_weight = (uint8_t*)dst;
+    for (int64_t i = 0; i < n / QK_I2_S; i++) {
+        for (int64_t j = 0; j < QK_I2_S; j++) {
+            int64_t group_idx = j / 16;
+            int64_t group_pos = j % 16;
+            uint8_t temp = (q8[i * QK_I2_S + j] << (6 - 2 * group_idx));
+            i2_weight[i * 16 + group_pos] |= temp;
+        }
+    }
+
+    free(q8);
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
+#else
+    GGML_UNUSED(src);
+    GGML_UNUSED(dst);
+    GGML_UNUSED(nrow);
+    GGML_UNUSED(n_per_row);
+    GGML_UNUSED(quant_weights);
+    GGML_ABORT("quantize_i2_sr requires an I2_S-enabled CPU backend");
+#endif
+}
+
 void ggml_vec_dot_i2_i8_s_1x1(int n, float * s, size_t bs, const void * vx, size_t bx, const void * vy, size_t by, int nrc) {
 #if defined(__AVX2__)
     const uint8_t *    x = (uint8_t *)vx;
@@ -1053,4 +1187,4 @@ void ggml_vec_dot_i2_i8_s(int n, float * s, size_t bs, const void * vx, size_t b
     {
         ggml_vec_dot_i2_i8_s_1x1(n, s, bs, vx, bx, vy, by, nrc);
     }
-}
\ No newline at end of file
+}
diff --git a/3rdparty/llama.cpp/ggml/include/ggml.h b/3rdparty/llama.cpp/ggml/include/ggml.h
index cbbdf06c..83bf749b 100644
--- a/3rdparty/llama.cpp/ggml/include/ggml.h
+++ b/3rdparty/llama.cpp/ggml/include/ggml.h
@@ -394,6 +394,7 @@ extern "C" {
         GGML_TYPE_I8_S    = 37,
         GGML_TYPE_TL1      = 38,
         GGML_TYPE_TL2      = 39,
+        GGML_TYPE_I2_SR    = 40,
         GGML_TYPE_COUNT,
     };
 
diff --git a/3rdparty/llama.cpp/ggml/src/ggml-quants.c b/3rdparty/llama.cpp/ggml/src/ggml-quants.c
index 127c6bcd..12f56147 100644
--- a/3rdparty/llama.cpp/ggml/src/ggml-quants.c
+++ b/3rdparty/llama.cpp/ggml/src/ggml-quants.c
@@ -16166,6 +16166,7 @@ bool ggml_validate_row_data(enum ggml_type type, const void * data, size_t nbyte
         case GGML_TYPE_I32:
         case GGML_TYPE_I64:
         case GGML_TYPE_I2_S:
+        case GGML_TYPE_I2_SR:
             // nothing to validate
             break;
         default:
@@ -16176,4 +16177,4 @@ bool ggml_validate_row_data(enum ggml_type type, const void * data, size_t nbyte
     }
 
     return true;
-}
\ No newline at end of file
+}
diff --git a/3rdparty/llama.cpp/ggml/src/ggml-quants.h b/3rdparty/llama.cpp/ggml/src/ggml-quants.h
index b0a7ad0c..b6e9cb5d 100644
--- a/3rdparty/llama.cpp/ggml/src/ggml-quants.h
+++ b/3rdparty/llama.cpp/ggml/src/ggml-quants.h
@@ -134,6 +134,7 @@ size_t quantize_tq1_0(const float * GGML_RESTRICT src, void * GGML_RESTRICT dst,
 size_t quantize_tq2_0(const float * GGML_RESTRICT src, void * GGML_RESTRICT dst, int64_t nrows, int64_t n_per_row, const float * imatrix);
 // size_t quantize_i2_s(const float * GGML_RESTRICT src, void * GGML_RESTRICT dst, int64_t nrow, int64_t n_per_row, const float * quant_weights);
 size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_row, const float * quant_weights);
+size_t quantize_i2_sr(const float * src, void * dst, int64_t nrow, int64_t n_per_row, const float * quant_weights);
 
 size_t quantize_q2_K(const float * GGML_RESTRICT src, void * GGML_RESTRICT dst, int64_t nrows, int64_t n_per_row, const float * imatrix);
 size_t quantize_q3_K(const float * GGML_RESTRICT src, void * GGML_RESTRICT dst, int64_t nrows, int64_t n_per_row, const float * imatrix);
@@ -153,4 +154,4 @@ void iq3xs_free_impl(int grid_size);
 
 #ifdef __cplusplus
 }
-#endif
\ No newline at end of file
+#endif
diff --git a/3rdparty/llama.cpp/ggml/src/ggml.c b/3rdparty/llama.cpp/ggml/src/ggml.c
index 121f72da..8c6ec086 100644
--- a/3rdparty/llama.cpp/ggml/src/ggml.c
+++ b/3rdparty/llama.cpp/ggml/src/ggml.c
@@ -1183,6 +1183,19 @@ static const struct ggml_type_traits type_traits[GGML_TYPE_COUNT] = {
         .vec_dot_type             = GGML_TYPE_I8_S,
         .nrows                    = 1,
     },
+    [GGML_TYPE_I2_SR] = {
+        .type_name                = "i2_sr",
+        .blck_size                = 1,
+        .type_size                = sizeof(int8_t),
+        .is_quantized             = true,
+        .to_float                 = (ggml_to_float_t) dequantize_row_i2_s,
+        .vec_dot                  = (ggml_vec_dot_t) ggml_vec_dot_i2_i8_s,
+        .gemv                     = (ggml_gemv_t) ggml_gemv_i2_i8_s,
+        .gemm                     = (ggml_gemm_t) ggml_gemm_i2_i8_s,
+        .nrows                    = 1,
+        .ncols                    = 4,
+        .vec_dot_type             = GGML_TYPE_I8_S,
+    },
     [GGML_TYPE_I8_S] = {
         .type_name                = "i8_s",
         .blck_size                = 1,
@@ -3485,6 +3498,9 @@ size_t ggml_nbytes(const struct ggml_tensor * tensor) {
         if(tensor->type == GGML_TYPE_I2_S || tensor->type == GGML_TYPE_TL1) {
             nbytes = nbytes / 4 + 32;
         }
+        else if(tensor->type == GGML_TYPE_I2_SR) {
+            nbytes = nbytes / 4 + ggml_nrows(tensor) * sizeof(float) + 32;
+        }
         else if (tensor->type == GGML_TYPE_TL2) {
             nbytes = (tensor->ne[0] - 256) * tensor->ne[1] / 3 * 5 / 8 + 256 * tensor->ne[1] / 2 * 4 / 8;
             if (nbytes % 32 != 0) nbytes = 32 - nbytes % 32 + nbytes;
@@ -12454,7 +12470,9 @@ static void ggml_compute_forward_mul_mat_one_chunk(
     // 16 * 2, accounting for mmla kernels
     float tmp[32];
 
-    const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+    const bool src0_is_i2_s  = src0->type == GGML_TYPE_I2_S;
+    const bool src0_is_i2_sr = src0->type == GGML_TYPE_I2_SR;
+    const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
     const float * act_scales = (const float*) ((const char *) wdata + (ne11 * ne10));
     const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
 
@@ -12501,7 +12519,7 @@ static void ggml_compute_forward_mul_mat_one_chunk(
                 //     }
                 // }
 
-                if (src0->type == GGML_TYPE_I2_S && iir0 + blck_0 - 1 < ir0_end) {
+                if ((src0_is_i2_s || src0_is_i2_sr) && iir0 + blck_0 - 1 < ir0_end) {
                     // 16 rows per vector dot product, so we can process 16 rows at a time blck == 16
                     // this is a bit of a hack, we should probably have a better way to handle this
                     vec_dot(ne00, &tmp[0], 1, 
@@ -12509,16 +12527,20 @@ static void ggml_compute_forward_mul_mat_one_chunk(
                         src1_col_de, 0, 16);
                     
                     // post compute activation scaling
+                    const int64_t scale_base = iir0 + i02 * ne01 + i03 * ne01 * ne02;
                     for (int row = 0; row < 16; row++) {
-                        tmp[row] = (tmp[row] - act_sums[i1]) / (act_scales[i1]) * (*scale);
+                        const float weight_scale = src0_is_i2_sr ? scale[scale_base + row] : scale[0];
+                        tmp[row] = (tmp[row] - act_sums[i1]) / (act_scales[i1]) * weight_scale;
                     }
                 }
                 else
                 {
                     for (int64_t ir0 = iir0; ir0 < iir0 + blck_0 && ir0 < ir0_end; ir0 += num_rows_per_vec_dot) {
-                        if (src0->type == GGML_TYPE_I2_S) {
+                        if (src0_is_i2_s || src0_is_i2_sr) {
                             vec_dot(ne00, &tmp[ir0 - iir0], 0, src0_row + ir0 * nb01 / 4, 0, src1_col_de, 0, 1);
-                            tmp[ir0 - iir0] = (tmp[ir0 - iir0]  - act_sums[i1]) / (act_scales[i1]) * (*scale);
+                            const int64_t scale_row = ir0 + i02 * ne01 + i03 * ne01 * ne02;
+                            const float weight_scale = src0_is_i2_sr ? scale[scale_row] : scale[0];
+                            tmp[ir0 - iir0] = (tmp[ir0 - iir0]  - act_sums[i1]) / (act_scales[i1]) * weight_scale;
                         } else {
                             vec_dot(ne00, &tmp[ir0 - iir0], (num_rows_per_vec_dot > 1 ? 16 : 0), src0_row + ir0 * nb01, (num_rows_per_vec_dot > 1 ? nb01 : 0), src1_col, (num_rows_per_vec_dot > 1 ? src1_col_stride : 0), num_rows_per_vec_dot);
                         }
@@ -13162,7 +13184,7 @@ UseGgmlGemm1:;
                     i11_processed = ne11 - ne11 % 4;
                 }
                 for (int64_t i11 = i11_processed + ith; i11 < ne11; i11 += nth) {
-                    if (src0->type == GGML_TYPE_I2_S) {
+                    if (src0->type == GGML_TYPE_I2_S || src0->type == GGML_TYPE_I2_SR) {
                         quantize_row_i8_s((float *)((char *) src1->data + i13*nb13 + i12*nb12 + i11*nb11), (void *) (wdata + i13*nbw3 + i12*nbw2 + i11*nbw1), ne10, act_scales + i11, act_sums + i11);
                     } else {
                         from_float((float *)((char *) src1->data + i13*nb13 + i12*nb12 + i11*nb11),
@@ -13255,21 +13277,29 @@ UseGgmlGemm2:;
         src0_end   = (src0_end   % matmul_num_cols) ? src0_end   + matmul_num_cols - (src0_end   % matmul_num_cols): src0_end;
         if (src0_start >= src0_end) return;
 
+        const bool src0_is_i2_s  = src0->type == GGML_TYPE_I2_S;
+        const bool src0_is_i2_sr = src0->type == GGML_TYPE_I2_SR;
+
         // If there are more than three rows in src1, use gemm; otherwise, use gemv.
         if (gemm && (ne11 > 3)) {
-            if (src0->type == GGML_TYPE_I2_S) {
-                float tmp[(src0_end - src0_start)*(ne11 - ne11 % 4)];
-                const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+            if (src0_is_i2_s || src0_is_i2_sr) {
+                const int64_t n_rows = src0_end - src0_start;
+                const int64_t n_cols = ne11 - ne11 % 4;
+                float * tmp = (float *) malloc((size_t) n_rows * n_cols * sizeof(float));
+                GGML_ASSERT(tmp != NULL);
+                const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
                 const float * act_scales = (const float*) ((const char *) src1_wdata + (ne11 * ne10));
                 const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
-                gemm(ne00, &tmp[0], src0_end - src0_start, (const char *) src0->data + src0_start * nb01 / 4,
-                    (const char *) src1_wdata, ne11 - ne11 % 4, src0_end - src0_start);
-                for (int col = 0; col < ne11 - ne11 % 4; col++) {
-                    for (int row = 0; row < src0_end - src0_start; row++) {
-                        tmp[col * (src0_end - src0_start) + row] = (tmp[col * (src0_end - src0_start) + row] - act_sums[col]) / (act_scales[col]) * (*scale);
+                gemm(ne00, &tmp[0], n_rows, (const char *) src0->data + src0_start * nb01 / 4,
+                    (const char *) src1_wdata, n_cols, n_rows);
+                for (int col = 0; col < n_cols; col++) {
+                    for (int row = 0; row < n_rows; row++) {
+                        const float weight_scale = src0_is_i2_sr ? scale[src0_start + row] : scale[0];
+                        tmp[col * n_rows + row] = (tmp[col * n_rows + row] - act_sums[col]) / (act_scales[col]) * weight_scale;
                     }
-                    memcpy((float *)((char *) dst->data + (col * nb1)) + src0_start, tmp + col * (src0_end - src0_start), (src0_end - src0_start) * sizeof(float));
+                    memcpy((float *)((char *) dst->data + (col * nb1)) + src0_start, tmp + col * n_rows, n_rows * sizeof(float));
                 }
+                free(tmp);
             }
             else {
                 gemm(ne00, (float *)((char *) dst->data) + src0_start, ne01, (const char *) src0->data + src0_start * nb01,
@@ -13277,19 +13307,23 @@ UseGgmlGemm2:;
             }
         }
         for (int iter = gemm ? ne11 - ne11 % 4 : 0; iter < ne11; iter++) {
-            if (src0->type == GGML_TYPE_I2_S) {
-                float tmp[src0_end - src0_start];
-                const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+            if (src0_is_i2_s || src0_is_i2_sr) {
+                const int64_t n_rows = src0_end - src0_start;
+                float * tmp = (float *) malloc((size_t) n_rows * sizeof(float));
+                GGML_ASSERT(tmp != NULL);
+                const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
                 const float * act_scales = (const float*) ((const char *) src1_wdata + (ne11 * ne10));
                 const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
                 gemv(ne00, &tmp[0], ne01,
                     (const char *) src0->data + src0_start * nb01 / 4,
                     (const char *) src1_wdata + (src1_col_stride * iter),
-                    1, src0_end - src0_start);
-                for (int row = 0; row < src0_end - src0_start; row++) {
-                    tmp[row] = (tmp[row] - act_sums[iter]) / (act_scales[iter]) * (*scale);
+                    1, n_rows);
+                for (int row = 0; row < n_rows; row++) {
+                    const float weight_scale = src0_is_i2_sr ? scale[src0_start + row] : scale[0];
+                    tmp[row] = (tmp[row] - act_sums[iter]) / (act_scales[iter]) * weight_scale;
                 }
-                memcpy((float *)((char *) dst->data + (iter * nb1)) + src0_start, tmp, (src0_end - src0_start) * sizeof(float));
+                memcpy((float *)((char *) dst->data + (iter * nb1)) + src0_start, tmp, n_rows * sizeof(float));
+                free(tmp);
             }
             else {
                 gemv(ne00, (float *)((char *) dst->data + (iter * nb1)) + src0_start, ne01,
@@ -14100,7 +14134,8 @@ static void ggml_compute_forward_get_rows_i2_s(
 
     const uint8_t * base = (const uint8_t *) src0->data;
     const size_t packed_bytes_total = (size_t) (ggml_nelements(src0) / 4);
-    const float  scl = *(const float *)(base + packed_bytes_total);
+    const float * scl = (const float *)(base + packed_bytes_total);
+    const bool row_scale = src0->type == GGML_TYPE_I2_SR;
 
     for (int64_t i = ir0; i < ir1; ++i) {
         const int64_t i12 = i/(ne11*ne10);
@@ -14110,9 +14145,11 @@ static void ggml_compute_forward_get_rows_i2_s(
 
         GGML_ASSERT(i01 >= 0 && i01 < ne01);
 
+        const int64_t scale_row = i01 + i11*ne01 + i12*ne01*ne11;
+
         dequantize_row_i2_s(
                 (const void *) ((char *) src0->data + i01*nb01/4 + i11*nb02/4 + i12*nb03/4),
-                     (float *) ((char *)  dst->data + i10*nb1  + i11*nb2  + i12*nb3), nc, scl);
+                     (float *) ((char *)  dst->data + i10*nb1  + i11*nb2  + i12*nb3), nc, row_scale ? scl[scale_row] : scl[0]);
     }
 }
 
@@ -14288,6 +14325,7 @@ static void ggml_compute_forward_get_rows(
                 ggml_compute_forward_get_rows_f32(params, dst);
             } break;
         case GGML_TYPE_I2_S:
+        case GGML_TYPE_I2_SR:
             ggml_compute_forward_get_rows_i2_s(params, dst);
             break;
         default:
@@ -14854,6 +14892,7 @@ static void ggml_compute_forward_clamp(
         case GGML_TYPE_TQ1_0:
         case GGML_TYPE_TQ2_0:
         case GGML_TYPE_I2_S:
+        case GGML_TYPE_I2_SR:
         case GGML_TYPE_TL1:
         case GGML_TYPE_TL2:
         case GGML_TYPE_I8_S:
@@ -22655,6 +22694,7 @@ size_t ggml_quantize_chunk(
         case GGML_TYPE_TQ2_0:   result = quantize_tq2_0(src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
         // each quantize a row, will put a scale in next row first 4B, will diminish by next quantize.
         case GGML_TYPE_I2_S:    result = quantize_i2_s(src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
+        case GGML_TYPE_I2_SR:   result = quantize_i2_sr(src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
         case GGML_TYPE_IQ2_XXS: result = quantize_iq2_xxs(src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
         case GGML_TYPE_IQ2_XS:  result = quantize_iq2_xs (src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
         case GGML_TYPE_IQ3_XXS: result = quantize_iq3_xxs(src + start, (char *) dst + start_row * row_size, nrows, n_per_row, imatrix); break;
@@ -22691,6 +22731,8 @@ size_t ggml_quantize_chunk(
 
     if (type == GGML_TYPE_I2_S) {
         result = nrows * row_size / 4 + 32;
+    } else if (type == GGML_TYPE_I2_SR) {
+        result = nrows * row_size / 4 + nrows * sizeof(float) + 32;
     } else {
         GGML_ASSERT(result == nrows * row_size);
     }
diff --git a/3rdparty/llama.cpp/gguf-py/gguf/constants.py b/3rdparty/llama.cpp/gguf-py/gguf/constants.py
index e0abfccb..7a486ed1 100644
--- a/3rdparty/llama.cpp/gguf-py/gguf/constants.py
+++ b/3rdparty/llama.cpp/gguf-py/gguf/constants.py
@@ -1428,8 +1428,11 @@ class GGMLQuantizationType(IntEnum):
     Q4_0_8_8 = 33
     TQ1_0   = 34
     TQ2_0   = 35
+    I2_S    = 36
+    I8_S    = 37
     TL1     = 38
     TL2     = 39
+    I2_SR   = 40
 
 
 # TODO: add GGMLFileType from ggml_ftype in ggml.h
@@ -1476,6 +1479,10 @@ class LlamaFileType(IntEnum):
     MOSTLY_Q4_0_8_8      = 35  # except 1d tensors
     MOSTLY_TQ1_0         = 36  # except 1d tensors
     MOSTLY_TQ2_0         = 37  # except 1d tensors
+    MOSTLY_TL1           = 38
+    MOSTLY_TL2           = 39
+    MOSTLY_I2_S          = 40  # except 1d tensors
+    MOSTLY_I2_SR         = 41  # except 1d tensors; row-scale I2_S layout
 
     GUESSED              = 1024  # not specified in the model file
 
@@ -1554,8 +1561,11 @@ GGML_QUANT_SIZES: dict[GGMLQuantizationType, tuple[int, int]] = {
     GGMLQuantizationType.Q4_0_8_8:(32, 2 + 16),
     GGMLQuantizationType.TQ1_0:   (256, 2 + 4 * 13),
     GGMLQuantizationType.TQ2_0:   (256, 2 + 64),
+    GGMLQuantizationType.I2_S:     (4, 1),
+    GGMLQuantizationType.I8_S:     (1, 1),
     GGMLQuantizationType.TL1:      (4, 1),
     GGMLQuantizationType.TL2:      (4, 1),
+    GGMLQuantizationType.I2_SR:    (4, 1),
 }
 
 
diff --git a/3rdparty/llama.cpp/include/llama.h b/3rdparty/llama.cpp/include/llama.h
index b5d9aeb5..e2aab2f8 100644
--- a/3rdparty/llama.cpp/include/llama.h
+++ b/3rdparty/llama.cpp/include/llama.h
@@ -181,6 +181,7 @@ extern "C" {
         LLAMA_FTYPE_MOSTLY_TL1           = 38,
         LLAMA_FTYPE_MOSTLY_TL2           = 39,
         LLAMA_FTYPE_MOSTLY_I2_S          = 40, // except 1d tensors
+        LLAMA_FTYPE_MOSTLY_I2_SR         = 41, // except 1d tensors; row-scale I2_S layout
         LLAMA_FTYPE_GUESSED = 1024, // not specified in the model file
     };
 
diff --git a/3rdparty/llama.cpp/src/llama.cpp b/3rdparty/llama.cpp/src/llama.cpp
index 666fcc4d..faed39a0 100644
--- a/3rdparty/llama.cpp/src/llama.cpp
+++ b/3rdparty/llama.cpp/src/llama.cpp
@@ -4620,7 +4620,8 @@ struct llama_model_loader {
                 case GGML_TYPE_Q6_K:    ftype = LLAMA_FTYPE_MOSTLY_Q6_K;    break;
                 case GGML_TYPE_TQ1_0:   ftype = LLAMA_FTYPE_MOSTLY_TQ1_0;   break;
                 case GGML_TYPE_TQ2_0:   ftype = LLAMA_FTYPE_MOSTLY_TQ2_0;   break;
-                case GGML_TYPE_I2_S:   ftype = LLAMA_FTYPE_MOSTLY_I2_S;   break;
+                case GGML_TYPE_I2_S:    ftype = LLAMA_FTYPE_MOSTLY_I2_S;   break;
+                case GGML_TYPE_I2_SR:   ftype = LLAMA_FTYPE_MOSTLY_I2_SR;  break;
                 case GGML_TYPE_IQ2_XXS: ftype = LLAMA_FTYPE_MOSTLY_IQ2_XXS; break;
                 case GGML_TYPE_IQ2_XS:  ftype = LLAMA_FTYPE_MOSTLY_IQ2_XS;  break;
                 case GGML_TYPE_IQ2_S:   ftype = LLAMA_FTYPE_MOSTLY_IQ2_S;   break;
@@ -5369,6 +5370,7 @@ static std::string llama_model_ftype_name(llama_ftype ftype) {
         case LLAMA_FTYPE_MOSTLY_TQ1_0:    return "TQ1_0 - 1.69 bpw ternary";
         case LLAMA_FTYPE_MOSTLY_TQ2_0:    return "TQ2_0 - 2.06 bpw ternary";
         case LLAMA_FTYPE_MOSTLY_I2_S:    return "I2_S - 2 bpw ternary";
+        case LLAMA_FTYPE_MOSTLY_I2_SR:   return "I2_SR - 2 bpw ternary row-scale";
         case LLAMA_FTYPE_MOSTLY_IQ2_XXS:  return "IQ2_XXS - 2.0625 bpw";
         case LLAMA_FTYPE_MOSTLY_IQ2_XS:   return "IQ2_XS - 2.3125 bpw";
         case LLAMA_FTYPE_MOSTLY_IQ2_S:    return "IQ2_S - 2.5 bpw";
@@ -18392,7 +18394,8 @@ static ggml_type llama_tensor_get_type(quantize_state_internal & qs, ggml_type n
                      ftype == LLAMA_FTYPE_MOSTLY_IQ1_M) {
                 new_type = GGML_TYPE_Q5_K;
             }
-            else if (ftype == LLAMA_FTYPE_MOSTLY_TQ1_0 || ftype == LLAMA_FTYPE_MOSTLY_TQ2_0 || ftype == LLAMA_FTYPE_MOSTLY_I2_S) {
+            else if (ftype == LLAMA_FTYPE_MOSTLY_TQ1_0 || ftype == LLAMA_FTYPE_MOSTLY_TQ2_0 ||
+                     ftype == LLAMA_FTYPE_MOSTLY_I2_S  || ftype == LLAMA_FTYPE_MOSTLY_I2_SR) {
                 new_type = GGML_TYPE_F16;
             }
             else if (new_type != GGML_TYPE_Q8_0) {
@@ -18417,7 +18420,8 @@ static ggml_type llama_tensor_get_type(quantize_state_internal & qs, ggml_type n
                      new_type == GGML_TYPE_Q4_0_8_8) {
                 new_type = GGML_TYPE_Q4_0;
             }
-            else if (ftype == LLAMA_FTYPE_MOSTLY_TQ1_0 || ftype == LLAMA_FTYPE_MOSTLY_TQ2_0 || ftype == LLAMA_FTYPE_MOSTLY_I2_S) {
+            else if (ftype == LLAMA_FTYPE_MOSTLY_TQ1_0 || ftype == LLAMA_FTYPE_MOSTLY_TQ2_0 ||
+                     ftype == LLAMA_FTYPE_MOSTLY_I2_S  || ftype == LLAMA_FTYPE_MOSTLY_I2_SR) {
                 new_type = GGML_TYPE_F16;
             }
         }
@@ -18730,7 +18734,8 @@ static void llama_model_quantize_internal(const std::string & fname_inp, const s
         case LLAMA_FTYPE_MOSTLY_Q6_K:    default_type = GGML_TYPE_Q6_K;    break;
         case LLAMA_FTYPE_MOSTLY_TQ1_0:   default_type = GGML_TYPE_TQ1_0;   break;
         case LLAMA_FTYPE_MOSTLY_TQ2_0:   default_type = GGML_TYPE_TQ2_0;   break;
-        case LLAMA_FTYPE_MOSTLY_I2_S:   default_type = GGML_TYPE_I2_S;   break;
+        case LLAMA_FTYPE_MOSTLY_I2_S:    default_type = GGML_TYPE_I2_S;    break;
+        case LLAMA_FTYPE_MOSTLY_I2_SR:   default_type = GGML_TYPE_I2_SR;   break;
         case LLAMA_FTYPE_MOSTLY_IQ2_XXS: default_type = GGML_TYPE_IQ2_XXS; break;
         case LLAMA_FTYPE_MOSTLY_IQ2_XS:  default_type = GGML_TYPE_IQ2_XS;  break;
         case LLAMA_FTYPE_MOSTLY_IQ2_S:   default_type = GGML_TYPE_IQ2_XS;  break;
