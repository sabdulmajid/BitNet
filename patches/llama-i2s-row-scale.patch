diff --git a/src/ggml-bitnet-mad.cpp b/src/ggml-bitnet-mad.cpp
index 4ba9d65..7c9ba94 100644
--- a/src/ggml-bitnet-mad.cpp
+++ b/src/ggml-bitnet-mad.cpp
@@ -55,20 +55,22 @@ size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_
 
     int n = nrow * n_per_row;
 
-    // f32 -> q8
-    double max = 0;
-    for (int i = 0; i < n; ++i) {
-        max = fmax(max, (double)fabs((double)src[i]));
-    }
-    double i2_scale = max;
-
     uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
-    for (int i=0; i<n; i++) {
-        if (fabs((double)(src[i])) < 1e-6) {
-            q8[i] = 1;
-            continue;
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
         }
-        q8[i] = (double)src[i] * i2_scale > 0 ? 2 : 0;
     }
 
     memset(dst, 0, n * sizeof(uint8_t) / 4);
@@ -87,32 +89,32 @@ size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_
         }
     }
 
-    float* scale_ptr = (float*)((char*)i2_weight + n / 4);
-    scale_ptr[0] = i2_scale;
-
     free(q8);
 
     // 32B for alignment
-    return nrow * row_size / 4 + 32;
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
 #else
     assert((nrow % 4) == 0 && "quantize_i2_s_1x4 requires nrow % 4 == 0");
 
     size_t row_size = ggml_row_size(GGML_TYPE_I2_S, n_per_row);
     int64_t n = nrow * n_per_row;
 
-    double max = 0;
-    for (int64_t i = 0; i < n; ++i) {
-        max = fmax(max, (double)fabs((double)src[i]));
-    }
-    double i2_scale = max;
-
     uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
-    for (int64_t i=0; i<n; i++) {
-        if (fabs((double)(src[i])) < 1e-6) {
-            q8[i] = 1;
-            continue;
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
         }
-        q8[i] = (double)src[i] * i2_scale > 0 ? 2 : 0;
     }
 
     uint8_t* out = (uint8_t*)dst;
@@ -139,34 +141,32 @@ size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_
         }
     }
 
-    // store scale at the end of quantized data (same location pattern as quantize_i2_s)
-    float* scale_ptr = (float*)((char*)out + n / 4);
-    scale_ptr[0] = (float)i2_scale;
-
     free(q8);
 
     // return size (keep same formula as quantize_i2_s)
-    return nrow * row_size / 4 + 32;
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
 #endif
 #elif defined(__ARM_NEON)
     size_t row_size = ggml_row_size(GGML_TYPE_I2_S, n_per_row);
 
     int n = nrow * n_per_row;
 
-    // f32 -> q8
-    double max = 0;
-    for (int i = 0; i < n; ++i) {
-        max = fmax(max, (double)fabs((double)src[i]));
-    }
-    double i2_scale = max;
-
     uint8_t* q8 = (uint8_t*)malloc(n * sizeof(uint8_t));
-    for (int i=0; i<n; i++) {
-        if (fabs((double)(src[i])) < 1e-6) {
-            q8[i] = 1;
-            continue;
+    float* scale_ptr = (float*)((char*)dst + n / 4);
+    for (int64_t row = 0; row < nrow; row++) {
+        double row_max = 0;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            row_max = fmax(row_max, (double)fabs((double)src[row * n_per_row + col]));
+        }
+        scale_ptr[row] = (float)row_max;
+        for (int64_t col = 0; col < n_per_row; col++) {
+            const int64_t i = row * n_per_row + col;
+            if (fabs((double)(src[i])) < 1e-6) {
+                q8[i] = 1;
+                continue;
+            }
+            q8[i] = src[i] > 0 ? 2 : 0;
         }
-        q8[i] = (double)src[i] * i2_scale > 0 ? 2 : 0;
     }
 
     memset(dst, 0, n * sizeof(uint8_t) / 4);
@@ -185,13 +185,10 @@ size_t quantize_i2_s(const float * src, void * dst, int64_t nrow, int64_t n_per_
         }
     }
 
-    float* scale_ptr = (float*)((char*)i2_weight + n / 4);
-    scale_ptr[0] = i2_scale;
-
     free(q8);
 
     // 32B for alignment
-    return nrow * row_size / 4 + 32;
+    return nrow * row_size / 4 + nrow * sizeof(float) + 32;
 #endif
 }
 
@@ -1053,4 +1050,4 @@ void ggml_vec_dot_i2_i8_s(int n, float * s, size_t bs, const void * vx, size_t b
     {
         ggml_vec_dot_i2_i8_s_1x1(n, s, bs, vx, bx, vy, by, nrc);
     }
-}
\ No newline at end of file
+}
diff --git a/3rdparty/llama.cpp/ggml/src/ggml.c b/3rdparty/llama.cpp/ggml/src/ggml.c
index 121f72da..24fc7200 100644
--- a/3rdparty/llama.cpp/ggml/src/ggml.c
+++ b/3rdparty/llama.cpp/ggml/src/ggml.c
@@ -3482,7 +3482,10 @@ size_t ggml_nbytes(const struct ggml_tensor * tensor) {
         for (int i = 0; i < GGML_MAX_DIMS; ++i) {
             nbytes += (tensor->ne[i] - 1)*tensor->nb[i];
         }
-        if(tensor->type == GGML_TYPE_I2_S || tensor->type == GGML_TYPE_TL1) {
+        if(tensor->type == GGML_TYPE_I2_S) {
+            nbytes = nbytes / 4 + ggml_nrows(tensor) * sizeof(float) + 32;
+        }
+        else if(tensor->type == GGML_TYPE_TL1) {
             nbytes = nbytes / 4 + 32;
         }
         else if (tensor->type == GGML_TYPE_TL2) {
@@ -12454,7 +12457,7 @@ static void ggml_compute_forward_mul_mat_one_chunk(
     // 16 * 2, accounting for mmla kernels
     float tmp[32];
 
-    const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+    const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
     const float * act_scales = (const float*) ((const char *) wdata + (ne11 * ne10));
     const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
 
@@ -12509,8 +12512,9 @@ static void ggml_compute_forward_mul_mat_one_chunk(
                         src1_col_de, 0, 16);
                     
                     // post compute activation scaling
+                    const int64_t scale_base = iir0 + i02 * ne01 + i03 * ne01 * ne02;
                     for (int row = 0; row < 16; row++) {
-                        tmp[row] = (tmp[row] - act_sums[i1]) / (act_scales[i1]) * (*scale);
+                        tmp[row] = (tmp[row] - act_sums[i1]) / (act_scales[i1]) * scale[scale_base + row];
                     }
                 }
                 else
@@ -12518,7 +12522,8 @@ static void ggml_compute_forward_mul_mat_one_chunk(
                     for (int64_t ir0 = iir0; ir0 < iir0 + blck_0 && ir0 < ir0_end; ir0 += num_rows_per_vec_dot) {
                         if (src0->type == GGML_TYPE_I2_S) {
                             vec_dot(ne00, &tmp[ir0 - iir0], 0, src0_row + ir0 * nb01 / 4, 0, src1_col_de, 0, 1);
-                            tmp[ir0 - iir0] = (tmp[ir0 - iir0]  - act_sums[i1]) / (act_scales[i1]) * (*scale);
+                            const int64_t scale_row = ir0 + i02 * ne01 + i03 * ne01 * ne02;
+                            tmp[ir0 - iir0] = (tmp[ir0 - iir0]  - act_sums[i1]) / (act_scales[i1]) * scale[scale_row];
                         } else {
                             vec_dot(ne00, &tmp[ir0 - iir0], (num_rows_per_vec_dot > 1 ? 16 : 0), src0_row + ir0 * nb01, (num_rows_per_vec_dot > 1 ? nb01 : 0), src1_col, (num_rows_per_vec_dot > 1 ? src1_col_stride : 0), num_rows_per_vec_dot);
                         }
@@ -13258,18 +13263,22 @@ UseGgmlGemm2:;
         // If there are more than three rows in src1, use gemm; otherwise, use gemv.
         if (gemm && (ne11 > 3)) {
             if (src0->type == GGML_TYPE_I2_S) {
-                float tmp[(src0_end - src0_start)*(ne11 - ne11 % 4)];
-                const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+                const int64_t n_rows = src0_end - src0_start;
+                const int64_t n_cols = ne11 - ne11 % 4;
+                float * tmp = (float *) malloc((size_t) n_rows * n_cols * sizeof(float));
+                GGML_ASSERT(tmp != NULL);
+                const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
                 const float * act_scales = (const float*) ((const char *) src1_wdata + (ne11 * ne10));
                 const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
-                gemm(ne00, &tmp[0], src0_end - src0_start, (const char *) src0->data + src0_start * nb01 / 4,
-                    (const char *) src1_wdata, ne11 - ne11 % 4, src0_end - src0_start);
-                for (int col = 0; col < ne11 - ne11 % 4; col++) {
-                    for (int row = 0; row < src0_end - src0_start; row++) {
-                        tmp[col * (src0_end - src0_start) + row] = (tmp[col * (src0_end - src0_start) + row] - act_sums[col]) / (act_scales[col]) * (*scale);
+                gemm(ne00, &tmp[0], n_rows, (const char *) src0->data + src0_start * nb01 / 4,
+                    (const char *) src1_wdata, n_cols, n_rows);
+                for (int col = 0; col < n_cols; col++) {
+                    for (int row = 0; row < n_rows; row++) {
+                        tmp[col * n_rows + row] = (tmp[col * n_rows + row] - act_sums[col]) / (act_scales[col]) * scale[src0_start + row];
                     }
-                    memcpy((float *)((char *) dst->data + (col * nb1)) + src0_start, tmp + col * (src0_end - src0_start), (src0_end - src0_start) * sizeof(float));
+                    memcpy((float *)((char *) dst->data + (col * nb1)) + src0_start, tmp + col * n_rows, n_rows * sizeof(float));
                 }
+                free(tmp);
             }
             else {
                 gemm(ne00, (float *)((char *) dst->data) + src0_start, ne01, (const char *) src0->data + src0_start * nb01,
@@ -13278,18 +13287,21 @@ UseGgmlGemm2:;
         }
         for (int iter = gemm ? ne11 - ne11 % 4 : 0; iter < ne11; iter++) {
             if (src0->type == GGML_TYPE_I2_S) {
-                float tmp[src0_end - src0_start];
-                const float * scale      = (float * )((uint8_t*) (src0->data) + (ne00 * ne01 / 4));
+                const int64_t n_rows = src0_end - src0_start;
+                float * tmp = (float *) malloc((size_t) n_rows * sizeof(float));
+                GGML_ASSERT(tmp != NULL);
+                const float * scale      = (float * )((uint8_t*) (src0->data) + (ggml_nelements(src0) / 4));
                 const float * act_scales = (const float*) ((const char *) src1_wdata + (ne11 * ne10));
                 const int32_t * act_sums   = (const int32_t*) ((const char *) act_scales + (ne11) * sizeof(float));
                 gemv(ne00, &tmp[0], ne01,
                     (const char *) src0->data + src0_start * nb01 / 4,
                     (const char *) src1_wdata + (src1_col_stride * iter),
-                    1, src0_end - src0_start);
-                for (int row = 0; row < src0_end - src0_start; row++) {
-                    tmp[row] = (tmp[row] - act_sums[iter]) / (act_scales[iter]) * (*scale);
+                    1, n_rows);
+                for (int row = 0; row < n_rows; row++) {
+                    tmp[row] = (tmp[row] - act_sums[iter]) / (act_scales[iter]) * scale[src0_start + row];
                 }
-                memcpy((float *)((char *) dst->data + (iter * nb1)) + src0_start, tmp, (src0_end - src0_start) * sizeof(float));
+                memcpy((float *)((char *) dst->data + (iter * nb1)) + src0_start, tmp, n_rows * sizeof(float));
+                free(tmp);
             }
             else {
                 gemv(ne00, (float *)((char *) dst->data + (iter * nb1)) + src0_start, ne01,
@@ -14100,7 +14112,7 @@ static void ggml_compute_forward_get_rows_i2_s(
 
     const uint8_t * base = (const uint8_t *) src0->data;
     const size_t packed_bytes_total = (size_t) (ggml_nelements(src0) / 4);
-    const float  scl = *(const float *)(base + packed_bytes_total);
+    const float * scl = (const float *)(base + packed_bytes_total);
 
     for (int64_t i = ir0; i < ir1; ++i) {
         const int64_t i12 = i/(ne11*ne10);
@@ -14110,9 +14122,11 @@ static void ggml_compute_forward_get_rows_i2_s(
 
         GGML_ASSERT(i01 >= 0 && i01 < ne01);
 
+        const int64_t scale_row = i01 + i11*ne01 + i12*ne01*ne11;
+
         dequantize_row_i2_s(
                 (const void *) ((char *) src0->data + i01*nb01/4 + i11*nb02/4 + i12*nb03/4),
-                     (float *) ((char *)  dst->data + i10*nb1  + i11*nb2  + i12*nb3), nc, scl);
+                     (float *) ((char *)  dst->data + i10*nb1  + i11*nb2  + i12*nb3), nc, scl[scale_row]);
     }
 }
 
@@ -22690,7 +22704,7 @@ size_t ggml_quantize_chunk(
     }
 
     if (type == GGML_TYPE_I2_S) {
-        result = nrows * row_size / 4 + 32;
+        result = nrows * row_size / 4 + nrows * sizeof(float) + 32;
     } else {
         GGML_ASSERT(result == nrows * row_size);
     }
